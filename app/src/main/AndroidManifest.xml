<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- poss: SMS / usage-stats removed, notifications for Relay kept -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Always-on monitoring -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Lets PackageManager see the two default targets on API 30+ without
         QUERY_ALL_PACKAGES (Play-friendly). -->
    <queries>
        <package android:name="cn.tydic.ethiopay" />
        <package android:name="prod.cbe.birr" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Uncry">
        <!-- Main launcher entry is owned by the activity-aliases below (vanity
             names). MainActivity keeps no LAUNCHER filter so only one icon
             ever appears; explicit intents (e.g. notification tap) still work. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- Vanity launcher aliases: exactly one enabled at a time via
             AppAlias.apply(). Labels are placeholders (System / Telebirr /
             CBEBirr Plus) until the community finalizes the list. -->
        <activity-alias
            android:name=".AliasUncry"
            android:targetActivity=".MainActivity"
            android:label="@string/alias_uncry"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".AliasSystem"
            android:targetActivity=".MainActivity"
            android:label="@string/alias_system"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".AliasTelebirr"
            android:targetActivity=".MainActivity"
            android:label="@string/alias_telebirr"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".AliasCbeBirrPlus"
            android:targetActivity=".MainActivity"
            android:label="@string/alias_cbebirr_plus"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <service
            android:name=".AppMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Monitors target payment apps install state for Uncry." />
        </service>

        <!-- Autostart after boot / update. directBootAware so
             LOCKED_BOOT_COMPLETED is actually delivered. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
            <!-- Install/uninstall of the monitored apps: refresh the watch list. -->
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
